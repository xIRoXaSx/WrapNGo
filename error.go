package main

const (
	ErrUserInterrupt      = "interrupt received"
	ErrOperationInterrupt = "operation failed"
	ErrOperationFailed    = "necessary operation failed"
	ErrTimeout            = "timeout reached"
)
